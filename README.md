# DirectX12_Game_1

Minimal first-step DirectX 12 sample project.

## Build (Windows)

```powershell
cmake -S . -B build
cmake --build build --config Release
```

## Run

Run `build/Release/DirectX12_Game_1.exe` (or Debug equivalent).
The app opens a native window and verifies DirectX 12 device initialization.
